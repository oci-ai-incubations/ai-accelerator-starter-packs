spec:
  template:
    spec:
      volumes:
        - name: oci-config
          secret:
            secretName: oci-config-secret
      containers:
        - name: nvidia-blueprint-rag
          volumeMounts:
            - name: oci-config
              mountPath: /wallet
              readOnly: true
